# Periodic Table of Elements

Wanted to mess around with Vue and create a Periodic Table of Elements.

## Todos

- Find a way to create an animated version of the elements with orbiting electrons using [This Atomic Bohr Model package](https://www.npmjs.com/package/atomic-bohr-model). I'm running into a bunch of errors right now but I've seen others using it so I think it's just a user error.
- Clean up SCSS (Right now it's a pig sty 🐖).
- Use Vue Router to create a unique page for each element when a user clicks on one.
